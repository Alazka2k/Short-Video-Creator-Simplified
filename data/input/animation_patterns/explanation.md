# We need to create more animation patterns and add them as json. And we need to find a way to test them before we can use them. Maybe by temporarily have only the one json available in the folder.

## Possible options (later):
- We can add a table in the database for the patterns instead of having them as json files
